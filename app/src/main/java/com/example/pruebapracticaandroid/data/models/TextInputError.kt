package com.example.pruebapracticaandroid.data.models

enum class TextInputError {
    NO_ERROR,
    EMAIL_EMPTY,
    PASSWORD_EMPTY,
    REPEATED_PASSWORD_EMPTY,
    SHORT_PASSWORD,
    INVALID_EMAIL_FORMAT,
    INVALID_PASSWORD_FORMAT,
    NOT_MATCHING_PASSWORD
}